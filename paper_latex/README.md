# LaTeX Paper Package

Compile from this folder with:

```bash
pdflatex main.tex
bibtex main
pdflatex main.tex
pdflatex main.tex
```

Before submission:

- Replace placeholder authors/institution.
- Complete BibTeX metadata in `references.bib`.
- Confirm target venue formatting requirements.
- Move this folder to `paper/` in the project repo.
